function [fitresult, gof] = CPC_partition_WT_SPLRU_results(number_of_active_cores, normalized_cpc_hits)
%CREATEFIT(NUMBER_OF_CONTENDING_ACCESSES,COHE_FIRST)
%  Create a fit.
%
%  Data for 'untitled fit 1' fit:
%      X Input : number_of_contending_accesses
%      Y Output: cohe_first
%  Output:
%      fitresult : a fit object representing the fit.
%      gof : structure with goodness-of fit info.
%
%  See also FIT, CFIT, SFIT.

%  Auto-generated by MATLAB on 15-Oct-2014 17:40:37


%% Fit: 'untitled fit 1'.
[xData, yData] = prepareCurveData( number_of_active_cores, normalized_cpc_hits );

% Set up fittype and options.
ft = fittype( 'poly1' );
opts = fitoptions( ft );
opts.Lower = [-Inf -Inf -Inf -Inf -Inf];
opts.Upper = [Inf Inf Inf Inf Inf];
opts.Normalize = 'on';

% Fit model to data.
[fitresult, gof] = fit( xData, yData, ft, opts );

% Plot fit with data.
figure( 'Name', 'untitled fit 1' );
h = plot( fitresult,'k', xData, yData, 'predobs','k square', 0.95 );
legend( h, 'data', 'fitted curve', 'Lower and Upper bounds based on PI', 'Location', 'NorthWest' );
% Label axes
xlabel( 'number of active cores' );
ylabel( 'normalized cpc cache hits' );
grid off

